[pytest]
testpaths = tests
python_files = test_*.py
markers =
    smoke: mark test as smoke test
    regression: mark test as regression test
    uat: mark test as UAT test

addopts = -q --maxfail=1
testpaths = tests
junit_family = xunit2